[project]
name = "javelance"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12.5"
dependencies = [
    "numpy>=2.4.0",
    "plotly>=6.5.0",
]

[dependency-groups]
dev = [
    "ruff>=0.14.10",
]
